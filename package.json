{
  "name": "static-web-poc",
  "version": "1.0.0",
  "description": "Lightweight static site with design tokens and Markdown CMS",
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js --watch"
  },
  "dependencies": {
    "marked": "^12.0.0",
    "front-matter": "^4.0.2"
  }
}
